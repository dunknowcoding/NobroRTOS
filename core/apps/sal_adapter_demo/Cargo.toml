[package]
name = "sal-adapter-demo"
version.workspace = true
edition.workspace = true

[[bin]]
name = "sal_adapter_demo"
path = "src/main.rs"

[dependencies]
airon-adapter-robo-servo = { path = "../../adapters/robo-servo", default-features = false }
airon-adapter-sensor-stub = { path = "../../adapters/sensor-stub", default-features = false }
airon-hal = { path = "../../crates/nobro_hal", default-features = false }
airon-kernel = { path = "../../crates/nobro_kernel", features = ["hal-profile"] }
airon-sal = { path = "../../crates/nobro_sal" }
cortex-m = { version = "0.7", features = ["critical-section-single-core"] }
cortex-m-rt = "0.7"
defmt = "0.3"
defmt-rtt = "0.4"
panic-probe = { version = "0.3", features = ["print-defmt"] }

[features]
default = ["board-promicro-nosd"]
board-promicro-nosd = [
    "airon-hal/board-promicro-nosd",
    "airon-adapter-robo-servo/board-promicro-nosd",
    "airon-adapter-sensor-stub/board-promicro-nosd",
]
board-nicenano-s140 = [
    "airon-hal/board-nicenano-s140",
    "airon-adapter-robo-servo/board-nicenano-s140",
    "airon-adapter-sensor-stub/board-nicenano-s140",
]
