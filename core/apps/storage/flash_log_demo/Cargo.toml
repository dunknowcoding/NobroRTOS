[package]
name = "flash-log-demo"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

# Sensor data logging to on-chip flash (NVMC) on a development board: erase a page, log
# synthetic sensor samples, read back + verify integrity. Publishes
# NOBRO_FLASH_LOG_REPORT.
[[bin]]
name = "flash_log_demo"
path = "src/main.rs"
required-features = ["board-promicro-nosd"]

[[bin]]
name = "flash_log_demo_s140"
path = "src/main.rs"
required-features = ["board-promicro-s140"]

[dependencies]
nobro-hal = { path = "../../../crates/nobro_hal", default-features = false }
cortex-m = "0.7"
cortex-m-rt = "0.7"
defmt = "0.3"
defmt-rtt = "0.4"
panic-halt = "0.2"

[features]
default = ["board-promicro-nosd"]
board-promicro-nosd = ["nobro-hal/board-promicro-nosd"]
board-promicro-s140 = ["nobro-hal/board-promicro-s140"]
board-nicenano-s140 = ["nobro-hal/board-nicenano-s140"]
