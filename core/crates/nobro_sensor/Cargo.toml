[package]
name = "nobro-sensor"
version.workspace = true
edition.workspace = true
# No-heap sensor utilities: health/fault detection (M47), offset calibration (M48),
# and sample-rate decimation (M49).
