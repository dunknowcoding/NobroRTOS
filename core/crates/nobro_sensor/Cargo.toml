[package]
name = "nobro-sensor"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
# No-heap sensor utilities: health/fault detection, offset calibration,
# and sample-rate decimation.

[dependencies]
nobro-device = { path = "../nobro_device" }
