[package]
name = "nobro-kernel"
version.workspace = true
edition.workspace = true

[dependencies]
nobro-hal = { path = "../nobro_hal", default-features = false, features = ["contract-only"], optional = true }

[features]
default = []
hal-profile = ["dep:nobro-hal"]
