[package]
name = "airon-kernel"
version.workspace = true
edition.workspace = true

[dependencies]
airon-hal = { path = "../airon-hal", default-features = false, features = ["contract-only"], optional = true }

[features]
default = []
hal-profile = ["dep:airon-hal"]
