# NobroRTOS Packages

This folder contains developer-platform package surfaces.

Planned package targets:

- `arduino/` for Arduino IDE library layout
- `platformio/` for PlatformIO package layout

Generated package archives, build outputs, and local cache files must stay out
of the repository.
