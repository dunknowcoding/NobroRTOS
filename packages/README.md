# NobroRTOS Packages

This folder contains developer-platform package surfaces.

Package targets:

- `arduino/` for Arduino IDE library metadata and include forwarding
- `platformio/` for PlatformIO library metadata and include forwarding
- `block-editor/` for a static visual app builder that emits `app.json`
- `web-flasher/` for a static no-install browser firmware handoff tool

Generated package archives, build outputs, and local cache files must stay out
of the repository.
