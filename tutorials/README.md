# NobroRTOS Tutorials

Tutorials are small, public, software-only exercises. They are meant to teach
the contract model before a user selects a physical board or adapter stack.

Run the tutorial gate:

```powershell
python tools/tutorial_runner.py
```

Available tutorials:

- `hello-device`: validate a board, actuator, sensor, and behavior contract.
