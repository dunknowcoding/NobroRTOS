# Integration checks

Portable integration gates for Arduino facades, adapters, wireless, audio,
camera, peripherals, and ROS. These do not contain local hardware identities or
raw laboratory evidence.

`check_portable_adapter_contract.py` keeps the common adapter lifecycle,
portable MPU9250 transport boundary, conformance app, catalog, and CI matrix in
sync.
