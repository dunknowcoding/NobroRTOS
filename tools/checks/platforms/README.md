# Platform checks

Board-profile, provider-tier, generated-firmware, and cross-target portability
gates. The firmware-generation gate exercises every profile that declares an
`application-image` contract.
